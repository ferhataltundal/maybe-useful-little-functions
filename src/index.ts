import { createMap, callMap, hasKey, deleteMap } from "./map";
import deeper from "./finder";
import sorting from "./sort";
import generate from "./generate";
import runner from "./run";
import range from "./range";
export default {
  deeper,
  sorting,
  generate,
  runner,
  range,
  createMap,
  callMap,
  hasKey,
  deleteMap,
};
